# Dumpling Dex — Privacy

Hosts the public privacy policy for the Dumpling Dex iOS app.
Live page: enable GitHub Pages (Settings → Pages → branch `main`, folder `/root`).
The app's source code lives in a separate private repository.
